# 位运算用途

## 基础运算

1. 位运算常见坑 :
- 优先级, 基本上最低级, 每次使用都加括号
- 移位越界, 每次写1ULL << bit就好
- 掩码构造, 注意(1ULL << 64)不可以, 超过ULL范围了, 用~0ULL

2. 位运算常见使用:
```cpp
x |= (1ULL << k) //置位
x &= (~ (1ULL << k)) // 清位
x ^= (1ULL << k) // 翻转
```
3. ~的常见使用: 把所有二进制位取反
- ~0ULL // 64 位全 1
- ~0u // 32 位全 1