
#include<bits/stdc++.h>
#define int long long
#define debug(x) cerr << #x << ": " << x << '\n';
using namespace std;
const int mod = 998244353;

void ovo() {   
    // pre: 区间段, 数组长度区间
    // lowerb: 找到段, pos = (x - pre) % ch, ch是数组重复次数
    // 找到谁 : set -> pos
    // m中的出现次数 : 0 2 3 5...次数
    // 1. ch
    // 2. 每一个去区间的递增数组, 类似于前缀和, 依次合并

    
}

signed main(){
    ios::sync_with_stdio(false); cin.tie(0);
    int _=1;
    // cin>>_;

    while(_--)  ovo();  return 0;
}