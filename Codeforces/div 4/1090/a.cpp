#include <bits/stdc++.h>
#define int long long
using namespace std;

void ovo() {
    int x; cin >> x;
    if (x == 67) {
        cout << 67 << '\n';
    } else {
        cout << x + 1 << '\n';
    }
}

signed main() {
    ios::sync_with_stdio(false); cin.tie(0);
    int _=1;
    cin>>_;

    while (_--) ovo();
}