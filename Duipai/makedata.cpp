#include<bits/stdc++.h>
using namespace std;
const int MAX = 1e5;

signed main(){
    ios::sync_with_stdio(false);
    cin.tie(0),cout.tie(0);
    freopen("a.in", "w", stdout);
    srand(time(0));

    cout << rand() % MAX << ' ' << rand() % MAX << endl;
    
    return 0;
}
